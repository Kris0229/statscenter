# Frontend environment variables. Copy to `.env` and adjust.
# Vite exposes only variables prefixed with VITE_ to the client.

# The FastAPI backend base URL, INCLUDING the `/api/v1` prefix.
VITE_API_BASE_URL=http://localhost:8000/api/v1
